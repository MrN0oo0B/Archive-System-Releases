# واجهة التفعيل

تمت إضافة بطاقة **تفعيل البرنامج** إلى صفحة الإعدادات للمستخدمين والمديرين.

- عند عدم وجود ترخيص: يظهر حقل مفتاح `XXXX-XXXX-XXXX-XXXX`، وعدد أيام التجربة المتبقية، ومعرّف الجهاز، وزر مراسلة شراء الترخيص.
- عند وجود ترخيص: تظهر حالة التفعيل، نوع الترخيص، المفتاح بشكل مخفي، تاريخ التفعيل، تاريخ الانتهاء، الأيام المتبقية، ومعرّف الجهاز.
- التفعيل يعمل محليًا دون اتصال بالإنترنت عبر واجهة Electron الحالية.
