# Archive System — التحديث التلقائي

تم استبدال نظام التحديث المحلي السابق بنظام تحديث حقيقي عبر GitHub Releases.

## طريقة العمل

1. أنشئ مستودع GitHub **عام** مخصص لتحديثات Archive System.
2. قبل بناء النسخة التي ستوزعها للمستخدمين، اضبط متغير البيئة:

```text
ARCHIVE_UPDATE_REPOSITORY=YOUR_GITHUB_USER/YOUR_REPOSITORY
```

3. ابنِ Installer من نوع NSIS:

```text
bun run electron:installer:win
```

4. داخل البرنامج، يدخل المؤسس المستودع نفسه وGitHub Personal Access Token في لوحة تحديث البرنامج.
5. يختار ملف `Archive-System-Setup-X.Y.Z.exe` ويكتب ملاحظات الإصدار كنقاط.
6. البرنامج ينشئ GitHub Release ويرفع ملف التثبيت و`latest.json` تلقائياً.
7. أجهزة المستخدمين تفحص آخر Release في الخلفية.
8. عند وجود إصدار أحدث تظهر نافذة التحديث، ويستطيع المستخدم تنزيله وتثبيته.

## البيانات وقاعدة البيانات

التحديث يتم على ملفات التطبيق فقط. قاعدة البيانات والملفات والإعدادات الموجودة داخل `%APPDATA%\\ArchiveSystem` لا يتم حذفها بواسطة عملية التحديث.

## ملاحظة مهمة

يجب أن يكون مستودع التحديث العام متاحاً للمستخدمين؛ لا يحتاج جهاز المؤسس إلى البقاء متصلاً بعد انتهاء عملية النشر.

لا تضع Personal Access Token داخل الكود أو داخل ملف التثبيت. يستخدمه البرنامج في جلسة النشر فقط.
